apiVersion: v1
kind: ConfigMap
metadata:
  name: secureflow-config
  namespace: secureflow
data:
  # CK-09 REMEDIATED — only non-sensitive configuration remains here.
  # Secrets now live in the secureflow-db-secrets Secret (and in Vault for
  # the injected app deployments, per Day 6).
  AUTH_SERVICE_URL: "http://auth-service:5001"
  TRANSACTION_SERVICE_URL: "http://transaction-service:5002"