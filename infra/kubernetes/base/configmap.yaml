apiVersion: v1
kind: ConfigMap
metadata:
  name: secureflow-config
  namespace: secureflow
data:
  # CK-09 — secrets in plaintext ConfigMap.
  # Remediation in Step 14: migrate to Vault Agent Injector.
  JWT_SECRET: "REDACTED-ROTATED-SECRET"
  SESSION_SECRET: "REDACTED-ROTATED-SECRET"
  AUTH_DB_PASSWORD: "REDACTED-ROTATED-SECRET"
  TX_DB_PASSWORD: "REDACTED-ROTATED-SECRET"
  AUTH_SERVICE_URL: "http://auth-service:5001"
  TRANSACTION_SERVICE_URL: "http://transaction-service:5002"
